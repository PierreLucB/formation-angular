import { BarrerTexteDirective } from './barrer-texte-directive';

describe('BarrerTexteDirective', () => {
  it('should create an instance', () => {
    const directive = new BarrerTexteDirective();
    expect(directive).toBeTruthy();
  });
});
